#!/bin/python3

# Find Lattice Path
def traverse_path(grid_size, curr_distance, path):
	if curr_distance = grid_size * 2:
		return path
	else:
		for i in range(2):
			return traverse_path(grid_size, )


def find_lattice_path(grid_size):

	# Number of steps it takes to get to the end
	distance = 2 * grid_size


	for i in range(grid_size):

def find_all_paths(dims):
	paths = []
	
	for i in range(dim):
		for j in range(dim):
			paths.append()



if __name__ == "__main__":
	lattice_dim = 2

	lattice_paths = find_all_paths(lattice_dim)

	print lattice_paths
